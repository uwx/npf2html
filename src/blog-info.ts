/** Information about a particular blog on Tumblr. */
export interface BlogInfo {
  /** The blog's UUID. */
  uuid: string;

  /** The blog's username. */
  name: string;

  /** The blog's URL. */
  url: string;
}
